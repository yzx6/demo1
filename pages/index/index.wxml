<!--index.wxml-->
<view class="container">
  <dl wx:for="{{message}}" wx:key="{{item.id}}">
    <dt><image src="{{item.img}}"/></dt>
    <dd>
    <p>{{item.name}}</p>
    <p>作者：{{item.author}}</p>
    <p>{{item.tag}}</p>
    </dd>
  </dl>
</view>
